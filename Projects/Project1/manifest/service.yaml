apiVersion: v1
kind: Service
metadata:
  name: game-app-service
  namespace: games
  labels:
    app: game-app
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: game-app